#include <stdio.h>
/**
 * main - prints all single digit numbers of base 10 from 0.
 *
 * Return: if runs successfully return 0
 */
int main(void)
{
int i;

for (i = 8; i < 18; i++)
{
putchar(i);
}
putchar('\n');
return (0);
}
